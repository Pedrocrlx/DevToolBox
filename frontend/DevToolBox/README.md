# DevToolBox UI

React + TypeScript + Vite + Tailwind CSS + shadcn/ui.

Run `npm ci` and `npm run dev`. Start the backend on port 8000; Vite proxies `/api` to it.
`npm run build` checks TypeScript and creates `dist/`. `npm run lint` runs ESLint.
See the root README for API and deployment instructions.
